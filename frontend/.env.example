# Google Maps JavaScript API key (optional) - used client-side to render the map.
# This key IS exposed to the browser; restrict it by HTTP referrer in
# Google Cloud Console (Credentials > this key > Application restrictions).
# Without it, itinerary cards still work, just without the map view.
VITE_GOOGLE_MAPS_API_KEY=
